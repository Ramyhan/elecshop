<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.elecshop.mapper.CartMapper">

	<insert id="insertItem">
		insert into tbl_cart
			(cno, pno, mid, cname, cimage, cprice, coption)
		values
			(seq_cno.nextval, #{pno}, #{mid}, #{cname}, #{cimage}, #{cprice}, #{coption})
	</insert>
	
	<select id="selectCartList" resultType="com.kh.elecshop.domain.CartVO">
		select *
		from tbl_cart
		where mid = #{mid}
	</select>
	
	<select id="selectCount" resultType="int">
		select count(*)
		from tbl_cart
		where mid = #{mid}
	</select>
	
	<delete id="deleteItem" parameterType="string">
		delete from tbl_cart
		where cno in (${value})
	</delete>

</mapper>
