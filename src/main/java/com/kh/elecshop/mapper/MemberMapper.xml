<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.elecshop.mapper.MemberMapper">
	<insert id="insertMember">
		insert into tbl_member
		(mno, mname, mid, mpw, memail, maddr, maddr_detail, mpost_code, mphone, mbirthday)
		values
		(seq_mno.nextval, #{mname}, #{mid}, #{mpw}, #{memail}, #{maddr}, #{maddr_detail}, #{mpost_code}, #{mphone}, #{mbirthday})
	</insert>
	
	<select id="login" resultType="com.kh.elecshop.domain.MemberVO">
		select * from tbl_member
		where mid = #{mid}
		and mpw = #{mpw}
	</select>
	
	<update id="updateMember">
		update tbl_member 
		set
		mname = #{mname},
		mpw = #{mpw},
		memail = #{memail},
		maddr = #{maddr},
		maddr_detail = #{maddr_detail},
		mphone = #{mphone},
		mbirthday = #{mbirthday}
		where mid = #{mid}
	</update>
	
	<update id="updatePoint">
		update tbl_member set
			mpoint = mpoint + #{ppoint}
		where mid = #{mid}
	</update>
	
	<select id="selectPoint" resultType="int">
		select mpoint from tbl_member
		where mid = #{mid}
	</select>
</mapper>
